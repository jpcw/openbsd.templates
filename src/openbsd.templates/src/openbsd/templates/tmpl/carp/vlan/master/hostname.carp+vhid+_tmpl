#if $carp_group
inet $ip $netmask $broadcast vhid $vhid carpdev vlan$vhid pass $password group $carp_group description "$desc"
#else
inet $ip $netmask $broadcast vhid $vhid carpdev vlan$vhid pass $password description "$desc"
#end if
