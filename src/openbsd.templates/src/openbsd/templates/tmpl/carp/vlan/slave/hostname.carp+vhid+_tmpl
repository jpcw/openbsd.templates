#if $carp_group 
inet $ip $netmask $broadcast vhid $vhid carpdev vlan$vhid advskew $advskew pass $password group $carp_group description "$desc"
#else
inet $ip $netmask $broadcast vhid $vhid carpdev vlan$vhid advskew $advskew pass $password description "$desc"
#end if
